{
  "name": "SecureLife",
  "install": "bash .cursor/install.sh",
  "start": "bash .cursor/start.sh",
  "terminals": [
    {
      "name": "backend",
      "command": "DATABASE_URL=\"${DATABASE_URL:-postgresql://securelife:securelife@localhost:5432/securelife}\" DASHBOARD_PASSWORD=\"${DASHBOARD_PASSWORD:-dev-dashboard-pass}\" PORT=\"${PORT:-10000}\" npm start",
      "description": "SecureLife Express API (PostgreSQL-backed) on port 10000. Add RESEND_API_KEY (email delivery) and GOOGLE_SHEETS_WEBHOOK_URL + SYNC_SECRET (Sheets sync) as secrets to enable those optional integrations."
    },
    {
      "name": "web",
      "command": "python3 -m http.server 8080 --bind 0.0.0.0",
      "description": "Static site server (index.html lead form, dashboard.html) on port 8080."
    }
  ],
  "ports": [
    { "name": "api", "port": 10000 },
    { "name": "web", "port": 8080 }
  ]
}
